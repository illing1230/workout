# 📦 Health App - 안드로이드 배포 가이드

이 문서는 Health App을 Google Play Store에 배포하는 전체 과정을 단계별로 설명합니다.

---

## 📋 목차

1. [사전 준비](#1-사전-준비)
2. [EAS 설정](#2-eas-설정)
3. [빌드 생성](#3-빌드-생성)
4. [Play Console 설정](#4-play-console-설정)
5. [첫 번째 업로드](#5-첫-번째-업로드)
6. [자동화 설정](#6-자동화-설정)
7. [업데이트 배포](#7-업데이트-배포)
8. [문제 해결](#8-문제-해결)

---

## 1. 사전 준비

### 1.1 필수 계정

✅ **Expo 계정**
```bash
# Expo 계정 생성
https://expo.dev/signup

# 로그인 확인
npx expo whoami
```

✅ **Google Play Developer 계정**
- URL: https://play.google.com/apps/publish/signup/
- 비용: $25 USD (1회)
- 처리 시간: 즉시

### 1.2 소프트웨어 설치

```bash
# Node.js 확인 (v18 이상 권장)
node --version

# EAS CLI 설치
npm install -g eas-cli

# 버전 확인
eas --version
```

### 1.3 프로젝트 확인

```bash
cd /home/remote/projects/apps/health-app

# 의존성 설치 (아직 안했다면)
npm install

# 앱 정상 동작 확인
npm start
```

---

## 2. EAS 설정

### 2.1 EAS 로그인

```bash
# EAS CLI 로그인
eas login

# 로그인 확인
eas whoami
```

### 2.2 프로젝트 초기화

```bash
# EAS 프로젝트 설정 (이미 eas.json이 있다면 생략)
eas build:configure
```

### 2.3 app.json 확인

다음 필드가 올바르게 설정되었는지 확인:

```json
{
  "expo": {
    "name": "Health App",
    "android": {
      "package": "com.healthapp.fitness",  // 고유해야 함!
      "versionCode": 1                     // 정수, 1부터 시작
    },
    "version": "1.0.0",                     // 사용자에게 표시되는 버전
    "extra": {
      "eas": {
        "projectId": "your-project-id"    // eas build:configure 시 자동 생성
      }
    }
  }
}
```

**중요**:
- `android.package`는 앱 출시 후 변경할 수 없습니다!
- 고유한 이름을 사용하세요 (예: `com.yourcompany.healthapp`)

---

## 3. 빌드 생성

### 3.1 프로덕션 빌드 (AAB)

```bash
# 안드로이드 프로덕션 빌드
eas build --platform android --profile production

# 빌드 진행 상황 확인
# EAS가 제공하는 URL에서 실시간 로그 확인 가능
```

**첫 빌드 시 질문들**:

1. **"Would you like to automatically create an EAS project?"**
   - `Yes` 선택

2. **"Generate a new Android Keystore?"**
   - `Yes` 선택 (EAS가 자동으로 관리)

3. **"Set up Google Service Account?"**
   - `Not now` 선택 (나중에 설정)

### 3.2 빌드 완료 대기

```bash
# 빌드 상태 확인
eas build:list

# 일반적인 빌드 시간: 5-20분
```

### 3.3 AAB 다운로드

빌드 완료 후:

1. **방법 1**: Expo 대시보드에서 다운로드
   - https://expo.dev/accounts/[your-account]/projects/health-app/builds
   - Download 버튼 클릭

2. **방법 2**: CLI로 다운로드
   ```bash
   # 최신 빌드 URL 확인
   eas build:list

   # 직접 다운로드 (URL 제공됨)
   ```

---

## 4. Play Console 설정

### 4.1 앱 생성

1. [Play Console](https://play.google.com/console)에 로그인
2. **"앱 만들기"** 클릭
3. 기본 정보 입력:
   - **앱 이름**: Health App
   - **기본 언어**: 한국어
   - **앱 또는 게임**: 앱
   - **무료 또는 유료**: 무료

### 4.2 스토어 등록 정보 작성

#### 📱 앱 세부정보
- **앱 이름**: Health App - AI 피트니스 트레이너
- **간단한 설명**: (PLAY_STORE_LISTING.md 참조, 80자)
- **전체 설명**: (PLAY_STORE_LISTING.md 참조, 4000자)
- **카테고리**: 건강 및 피트니스
- **이메일**: your-email@example.com

#### 🖼️ 그래픽
1. **앱 아이콘**: `assets/icon.png` (512x512)
2. **피처 그래픽**: 1024x500 이미지 생성 필요
3. **스크린샷**: 최소 2개, 권장 4-8개
   - 앱 실행 후 스크린샷 캡처
   - 1080x1920 권장

#### 🔗 연락처
- **웹사이트**: (선택사항)
- **이메일**: your-email@example.com (필수)
- **전화번호**: (선택사항)

### 4.3 개인정보 처리방침

**개인정보 처리방침 호스팅 (3가지 방법)**:

#### 방법 1: GitHub Pages (추천)
```bash
# GitHub 저장소 생성 후
# Settings → Pages → Source: main branch
# PRIVACY_POLICY.md를 HTML로 변환하여 업로드

# URL 예시:
https://yourusername.github.io/health-app/privacy-policy.html
```

#### 방법 2: Google Docs
1. PRIVACY_POLICY.md 내용을 Google Docs에 복사
2. 파일 → 공유 → 링크가 있는 모든 사용자에게 공개
3. URL 복사

#### 방법 3: 웹사이트
- 웹사이트가 있다면 `/privacy-policy` 페이지 생성

**Play Console에 URL 입력**:
- 스토어 설정 → 개인정보 처리방침 → URL 입력

### 4.4 콘텐츠 등급

1. **앱 콘텐츠** → **콘텐츠 등급** 선택
2. **설문 조사 시작**
3. 연락처 이메일 입력
4. 앱 카테고리: **유틸리티, 생산성, 커뮤니케이션 또는 기타**
5. 질문에 답변:
   - 폭력성: 없음
   - 성적 콘텐츠: 없음
   - 언어: 적절함
   - 사용자 상호작용: 있음 (AI 챗봇)
   - 사용자 정보 공유: 없음
6. **등급 받기** 클릭

### 4.5 데이터 안전성

1. **앱 콘텐츠** → **데이터 안전성** 선택
2. **데이터 수집 및 보안 관행 선언**
3. 질문에 답변:

**앱에서 사용자 데이터를 수집하거나 공유하나요?**
- **아니요** 선택

**이유**: 모든 데이터가 로컬(기기)에만 저장됨

4. 개인정보 처리방침 URL 입력
5. **제출**

### 4.6 타겟 오디언스 및 콘텐츠

1. **타겟 오디언스**
   - **연령 그룹**: 13세 이상

2. **뉴스 앱**
   - **아니요** 선택

3. **COVID-19 관련 앱**
   - **아니요** 선택

4. **광고**
   - **아니요** 선택 (광고 없음)

---

## 5. 첫 번째 업로드

### 5.1 수동 업로드 (필수)

**중요**: Google Play API 제한으로 첫 업로드는 반드시 수동으로 해야 합니다!

1. Play Console → **테스트** → **내부 테스트** 선택
2. **새 버전 만들기** 클릭
3. **Android App Bundle** 섹션에서 **업로드** 클릭
4. 다운로드한 AAB 파일 선택
5. **버전 이름**: `1.0.0` (app.json의 version과 일치)
6. **출시 노트** 작성:
   ```
   첫 번째 릴리스
   - 운동 기록 및 추적 기능
   - AI 트레이너 챗봇
   - 10가지 운동 라이브러리
   - 5가지 체계적인 프로그램
   - 다크 테마 지원
   ```
7. **버전 검토** → **출시** 클릭

### 5.2 테스터 추가 (신규 계정 필수)

**2023년 11월 13일 이후 생성된 계정**: 프로덕션 출시 전에 반드시 수행

1. **테스터** 탭 선택
2. **테스터 목록 만들기**
3. 최소 **12명의 테스터** 이메일 추가
4. **14일 동안 테스트** 진행
5. 테스트 완료 후 프로덕션 출시 가능

### 5.3 프로덕션 출시

테스트 완료 후:

1. **프로덕션** 탭 선택
2. **새 버전 만들기**
3. 내부 테스트 버전을 프로덕션으로 승격
4. 국가 선택 (전 세계 또는 특정 국가)
5. **버전 검토** → **출시** 클릭
6. **검토 대기** (2-7일 소요)

---

## 6. 자동화 설정

첫 수동 업로드 완료 후, 향후 업데이트를 자동화할 수 있습니다.

### 6.1 Google Service Account 생성

#### Step 1: Google Cloud Project 생성
1. [Google Cloud Console](https://console.cloud.google.com/) 접속
2. **프로젝트 만들기**
3. 프로젝트 이름: `health-app-playstore`

#### Step 2: API 활성화
1. **API 및 서비스** → **라이브러리**
2. **Google Play Android Developer API** 검색
3. **사용 설정** 클릭

#### Step 3: Service Account 생성
1. **IAM 및 관리자** → **서비스 계정**
2. **서비스 계정 만들기**
3. 이름: `health-app-deployer`
4. 역할: **서비스 계정 사용자**
5. **완료** 클릭

#### Step 4: JSON 키 생성
1. 생성된 서비스 계정 클릭
2. **키** 탭 → **키 추가** → **새 키 만들기**
3. 유형: **JSON** 선택
4. 키 다운로드 (자동으로 다운로드됨)
5. 파일명: `google-service-account.json`

#### Step 5: Play Console 권한 부여
1. Play Console → **설정** → **API 액세스**
2. **서비스 계정** 섹션에서 방금 만든 계정 찾기
3. **액세스 권한 부여** 클릭
4. 권한 선택:
   - **버전 만들기 및 수정**
   - **앱 정보 보기**
5. **초대 보내기** 클릭

⚠️ **중요**: 권한이 활성화되기까지 최대 24시간 소요

### 6.2 JSON 키 저장

```bash
# 프로젝트 루트에 저장 (gitignore에 추가됨)
mv ~/Downloads/health-app-*.json ./google-service-account.json

# .gitignore에 추가 (이미 추가되어 있어야 함)
echo "google-service-account.json" >> .gitignore
```

### 6.3 EAS Submit 설정

```bash
# eas.json에 이미 설정되어 있음:
# "submit": {
#   "production": {
#     "android": {
#       "serviceAccountKeyPath": "./google-service-account.json",
#       "track": "internal"
#     }
#   }
# }

# 자동 제출 테스트
eas submit --platform android --profile production
```

---

## 7. 업데이트 배포

### 7.1 버전 관리

앱 업데이트 시 두 가지 번호를 관리해야 합니다:

1. **versionCode** (숫자): Play Store가 업데이트 판단에 사용
2. **version** (문자열): 사용자에게 표시되는 버전

```json
// app.json
{
  "expo": {
    "version": "1.0.0",        // 사용자 표시용
    "android": {
      "versionCode": 1         // Play Store용 (정수)
    }
  }
}
```

### 7.2 업데이트 시나리오

#### 시나리오 1: 버그 수정 (JavaScript만)

OTA (Over-The-Air) 업데이트 사용:

```bash
# version 업데이트
# "version": "1.0.0" → "1.0.1"

# EAS Update로 배포
eas update --branch production --message "버그 수정"

# versionCode는 변경하지 않음
# Play Store 재출시 불필요
```

#### 시나리오 2: 새로운 기능 추가 (네이티브 변경)

Store 업데이트 필요:

```bash
# 1. app.json 업데이트
# "version": "1.0.0" → "1.1.0"
# "versionCode": 1 → 2

# 2. 새 빌드 생성
eas build --platform android --profile production

# 3. Play Store 제출
eas submit --platform android --profile production

# 또는 수동 업로드
```

#### 시나리오 3: 주요 업데이트

```bash
# 1. app.json 업데이트
# "version": "1.1.0" → "2.0.0"
# "versionCode": 2 → 3

# 2. 빌드 및 제출
eas build --platform android --profile production
eas submit --platform android --profile production
```

### 7.3 출시 노트 작성

각 업데이트마다 출시 노트를 명확히 작성:

```
버전 1.1.0

새로운 기능:
- 운동 타이머 추가
- 운동 영상 가이드
- 프로필 사진 업로드

개선사항:
- AI 챗봇 응답 속도 향상
- UI 애니메이션 개선

버그 수정:
- 캘린더 날짜 선택 오류 수정
- 통계 차트 표시 오류 해결
```

### 7.4 단계적 출시 (Staged Rollout)

안정성을 위해 단계적 출시 권장:

1. Play Console → 프로덕션 → 새 버전 생성
2. **단계적 출시** 선택
3. 비율 선택:
   - 1% → 5% → 10% → 20% → 50% → 100%
4. 각 단계마다 1-2일 대기
5. 문제 없으면 다음 단계로 확대

---

## 8. 문제 해결

### 8.1 일반적인 오류

#### 오류: "Build failed: Android Keystore not found"

**해결책**:
```bash
# EAS가 자동으로 Keystore 생성하도록 허용
eas build --platform android --profile production
# "Generate new keystore?" → Yes
```

#### 오류: "Package name already exists"

**해결책**:
```json
// app.json에서 고유한 package 이름 사용
"android": {
  "package": "com.yourcompany.healthapp"  // 변경
}
```

#### 오류: "versionCode XX already exists"

**해결책**:
```json
// app.json에서 versionCode 증가
"android": {
  "versionCode": 2  // 이전 값보다 크게
}
```

### 8.2 Play Console 거부 사유

#### 거부: "Missing Privacy Policy"

**해결책**:
- PRIVACY_POLICY.md를 웹사이트에 호스팅
- Play Console → 개인정보 처리방침 URL 입력

#### 거부: "Incomplete Data Safety Section"

**해결책**:
- 앱 콘텐츠 → 데이터 안전성 → 모든 질문 답변
- "데이터 수집 안 함" 선택

#### 거부: "Missing Content Rating"

**해결책**:
- 앱 콘텐츠 → 콘텐츠 등급 → 설문 완료

### 8.3 빌드 시간 단축

```bash
# 캐시 사용으로 빌드 시간 단축
eas build --platform android --profile production --clear-cache

# 로컬 자격증명 사용 (고급)
eas build:configure
# "local" credentials 선택
```

### 8.4 로그 확인

```bash
# 빌드 로그 확인
eas build:list
# URL 클릭하여 상세 로그 확인

# 제출 상태 확인
eas submit:list
```

---

## 📊 빠른 명령어 참조

```bash
# 로그인
eas login

# 빌드
eas build --platform android --profile production

# 제출 (서비스 계정 설정 후)
eas submit --platform android --profile production

# OTA 업데이트
eas update --branch production --message "업데이트 메시지"

# 상태 확인
eas build:list
eas submit:list

# 자격증명 관리
eas credentials
```

---

## ⏱️ 예상 일정

| 단계 | 소요 시간 |
|------|----------|
| EAS 설정 | 10-15분 |
| 첫 빌드 | 10-20분 |
| Play Console 설정 | 1-2시간 |
| 수동 업로드 | 10분 |
| 내부 테스트 | 즉시 |
| 프로덕션 검토 | 2-7일 |
| **총 첫 출시** | **3-10일** |

---

## 📚 유용한 링크

- [EAS Build 문서](https://docs.expo.dev/build/introduction/)
- [EAS Submit 문서](https://docs.expo.dev/submit/introduction/)
- [Play Console 도움말](https://support.google.com/googleplay/android-developer)
- [Android 출시 체크리스트](https://developer.android.com/distribute/best-practices/launch/launch-checklist)

---

## 🆘 지원

문제가 발생하면:

1. [Expo Forums](https://forums.expo.dev/)
2. [Stack Overflow](https://stackoverflow.com/questions/tagged/expo)
3. [Play Console 지원](https://support.google.com/googleplay/android-developer/gethelp)

---

**마지막 업데이트**: 2025-11-20
**버전**: 1.0.0
